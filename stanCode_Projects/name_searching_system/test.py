def main():
    matching_names = []
    target = 'Aa'
    name_frag = target.lower
    # frag_len = len(str(name_frag))
    print(name_frag)
    # name_data = {'Jerry': 1111,
    #              'Aaron': 333,
    #              'Isaac': 999}
    # for name in name_data:
    #     name = name.lower
    #     name_len = len(str(name))
    #     for i in range(name_len - frag_len + 1):
    #         if str(name_frag) == str(name[0 + i:frag_len + i]):
    #             if name not in matching_names:
    #                 matching_names.append(name)
    # print(matching_names)


if __name__ == '__main__':
    main()
